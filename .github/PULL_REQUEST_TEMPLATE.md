## What does this PR do?

<!-- One paragraph or less. Link the issue it closes if applicable: "Closes #123" -->

## Checklist

- [ ] Tested with `make dev`
- [ ] `cargo build --manifest-path src-tauri/Cargo.toml` passes (if `src-tauri/` was changed)
- [ ] No new network calls beyond the OpenRouter API
- [ ] No new external dependencies added without prior discussion
